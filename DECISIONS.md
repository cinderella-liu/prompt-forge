# Prompt Forge 决策记录

## 当前阶段

- 当前可测试版本：v0.6
- 阶段目标：朋友间测试，验证核心工作流是否成立
- 当前交付形态：Android debug APK
- 当前包名：`com.promptforge.app`

## 产品定位

Prompt Forge 是一个“意图到多模态提示词”的翻译器。

用户输入一句自然语言意图，App 生成四种提示词：

- 文本提示词
- 图片提示词
- 视频提示词
- 代码提示词

当前版本不调用真实模型，先用内置 schema 生成逻辑验证产品流程、信息结构和提示词可用性。

## 已确定的产品决策

1. App 名称使用 `Prompt Forge`。
2. 第一阶段先做 Web 单页，再通过 Capacitor 打包为 Android APK。
3. 先生成四种提示词，但允许某些模态输出“不适用”。
4. 图片和视频提示词采用“中文理解 + 英文执行稿”：
   - 中文用于用户判断 App 是否理解正确。
   - 英文用于复制到 Midjourney、Flux、DALL-E、Kling、Runway、Pika 等工具。
5. 意图解析能力保留，但默认隐藏：
   - 它是后台中间层，不是给普通用户看的最终结果。
   - 只保留“查看理解”入口，用户需要检查理解是否跑偏时再展开。
6. 历史记录放在页面最后，并默认折叠，避免页面越用越长。
7. 输入框下方只保留两个示例，避免喧宾夺主。
8. UI 风格采用复古活泼配色：
   - 复古蓝
   - 复古绿
   - 复古枣红
   - 米色/芥末黄辅助色
9. App 图标使用用户提供的复古蓝底“人脑 ↔ AI 芯片”图标。
10. 首屏交互做减法：
   - 输入框下方只保留两个可点击示例。
   - 输出类型明确为“支持多选”，用图标、文字、勾选状态表达。
   - 主按钮使用更通俗的“一键生成”。
   - 生成按钮在手机端固定在底部安全区，方便单手操作。
11. 视觉方向继续保持复古活泼，但降低背景装饰和大面积深色按钮的存在感，让输入和生成结果成为页面重点。
12. 2026-08-24 起，界面审美调整为更简洁的 Apple 风格：
   - 减少粗描边、重阴影和强装饰背景。
   - 使用浅色背景、柔和卡片、细分割线和克制功能色。
   - 历史记录增加单条删除功能，避免本地历史长期堆积。

## v0.6 已具备能力

- 一句话输入
- 模态选择：文本 / 图片 / 视频 / 代码
- 详细度选择：简洁 / 标准 / 详尽
- 四种提示词生成
- 不适用判断
- 每个提示词独立复制
- 每个提示词本地编辑
- 本地历史记录，最近 20 条
- 历史记录折叠
- 意图解析默认隐藏
- Android APK 安装测试

## 当前测试目标

朋友间测试时重点看三件事：

1. 四类提示词是否能直接复制到对应工具里使用。
2. “不适用”判断是否合理，是否减少无意义输出。
3. 手机端输入、阅读、复制、编辑是否顺手。

当前阶段不重点测试：

- 真实 LLM 调用质量
- 用户账号
- 云端同步
- API Key 管理
- 一键直连图片/视频/代码工具

## 后续可能方向

1. 接入真实 LLM：
   - 意图解析一次调用。
   - 文本、图片、视频、代码生成器分开调用。
2. 增加目标工具选择：
   - 图片：Midjourney / Flux / DALL-E / 通义万相。
   - 视频：Kling / Runway / Pika / Luma。
   - 代码：Cursor / Claude Code / Copilot。
3. 针对单个模态重新生成，而不是每次全量生成。
4. 增加“已有 prompt 诊断/补全”能力。
5. 将 debug APK 转为正式签名包。

## 继续迭代时先读

以后继续修改 Prompt Forge 时，先阅读本文件，再查看：

- `src/App.tsx`
- `src/styles.css`
- `android/`

不要只依赖聊天上下文记忆。
